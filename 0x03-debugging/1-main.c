#include<stdio.h>

/**
 * main - causes of an infinite loop
 * Return: 0
 */

int main(void)
{
	int i;

	print f(Infinite loop incoming :(/n");

			i = 0;
			/*
			 * while (i<10)
			 * {
			 * putchar (i);
			 * }
			 */
			print f(Infinite loop incoming :(/n");

				retuen(0);
				}
